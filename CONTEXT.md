# Bangumi 好友排序

本项目为 Bangumi 用户关系页面提供一致的好友排序语言，区分页面展示身份、稳定身份与派生的活跃状态。

## Language

**好友列表**：
Bangumi 用户的好友页或反向好友页中，由页面一次性提供的全部用户条目；即使好友数量很多，页面也不分页。

**好友**：
好友列表中的一个 Bangumi 用户，以用户标识作为稳定身份。
_Avoid_: 用户 ID、数字 ID

**展示名称**：
好友条目中直接呈现给访问者的名称；“名称”排序以此为主要排序值。
_Avoid_: 用户名、昵称、用户 ID

**用户标识**：
好友个人页 URL 中 `/user/` 后的稳定标识，可为文字或数字；用于识别好友。
_Avoid_: 展示名称、数字 ID

**网页默认顺序**：
Bangumi 返回好友列表时的原始条目顺序，是从旧到新的加好友顺序。多列页面按从上到下逐行、每行从左到右阅读；较早的好友处于更靠前的视觉位置。
_Avoid_: 已解析的加好友时间

**排序前顺序**：
当前页面内，一次排序开始前好友列表已有的排列顺序，是平局继承规则所保留的相对顺序；首次排序前为网页默认顺序，重新打开或刷新页面后重新起算。
_Avoid_: 上一个排序字段、跨页面排序历史

**可见好友**：
好友列表中当前实际展示的好友；被任何页面组件隐藏的好友条目不属于可见好友，但仍是好友列表的成员。可见好友集合随页面组件的筛选即时变化。
_Avoid_: 筛选结果、显示中的项

**名次**：
好友在当前排序目标与方向下重排后的可见好友序列中的展示位置，从 1 开始；名次在可见好友中恒唯一，且始终指视觉展示顺序，不随方向翻转含义。
_Avoid_: 排名值、评分、排序值

**上次活跃**：
从好友时间胶囊默认动态视图中，访问者可见的任意类型最新动态所取得的时间；“上次活跃”排序按时间从近到远排列。
_Avoid_: 登录时间、在线状态

**活跃时刻**：
上次活跃的规范时间点，以整数 Unix 秒表达；页面未披露秒时，以该分钟的第 0 秒代表。
_Avoid_: 真实秒级时间、毫秒时间戳

**无公开动态**：
成功读取好友的时间胶囊，但其中没有任何访问者可见的动态。这是一种可靠的查询结果，不代表好友从未活跃。
_Avoid_: 从未活跃、查询失败

**未知活跃时间**：
因请求或页面解析失败，且没有旧查询结果可用，无法确定好友上次活跃时间的状态。
_Avoid_: 无公开动态、从未活跃

**同步率**：
当前登录访问者与好友之间由 Bangumi 用户主页披露的共同喜好同步百分比；同一好友对不同访问者可有不同的同步率。
_Avoid_: 完成率、好友的固有同步率

**和我贴贴**：
以当前登录访问者收到的贴贴数量为依据的好友排序入口；“我”始终指当前登录访问者，不随被查看的好友列表所属用户改变。
_Avoid_: 好友收到的贴贴、列表所属用户收到的贴贴

**内容链接**：
时间胶囊动态实际承载内容的 Bangumi 链接；它用于识别同一份贴贴，不以动态显示名称判断内容是否相同。

**动态编号**：
时间胶囊条目提供的动态稳定标识；当内容链接未披露时，可作为该动态的次级去重依据。

**表情容器标识**：
时间胶囊条目承载贴贴数据的完整容器标识；当内容链接和动态编号均未披露时，可作为末级去重依据。

**贴贴数量**：
在统计范围涉及的吐槽与收藏动态上，某位用户给予当前登录访问者的表情数量，不包括回复及回复上的表情。动态按内容链接、动态编号、表情容器标识的顺序去重；同一动态同时披露多个标识时，这些标识关联为同一份贴贴。没有任何标识的动态不去重，直接累计。原站不允许一个人给同一条动态贴多个表情，因此同一用户在一条动态中贡献一次。
_Avoid_: 回复数、最近一段时间内新收到的表情数

**共同喜好数**：
当前登录访问者与好友之间由 Bangumi 用户主页披露的共同喜好条目数量；同一好友对不同访问者可有不同的共同喜好数。
_Avoid_: 共同好友数、完成条目数

**喜好契合**：
以同步率与共同喜好数两项指标聚合的远程排序入口，数据由 Bangumi 用户主页向当前登录访问者披露；同一好友对不同访问者可有不同的喜好契合数据。
_Avoid_: 好感度、契合度

**完成条目数**：
好友在 Bangumi 用户主页披露的、给定完成统计范围内状态为“完成”的条目数量。
_Avoid_: 条目完成数量、收藏数

**完成统计范围**：
完成条目数所覆盖的条目集合，包括聚合范围“全部”及动画、书籍、音乐、游戏、三次元五个分类。
_Avoid_: 将“全部”称为条目分类

**增量刷新**：
切换到需要远程数据的排序目标时，只获取该目标缺失或已过期的数据；好友级指标按好友补取，和我贴贴按完整统计范围重新获取。
_Avoid_: 缓存排序、全量刷新

**全量刷新**：
已经处于同一远程排序目标时通过两阶段点击主动触发，不考虑该目标的缓存是否有效，重新获取其全部数据；好友级指标覆盖当前好友列表，和我贴贴覆盖完整统计范围。
_Avoid_: 增量刷新、重新排序

**脚本元数据**：
发布脚本顶部 `==UserScript==` 头部块中声明脚本身份与安装信息的全部字段，包括名称、版本、简介、生效页面、运行时机与权限等；字段治理由人工决定，版本值由人工维护的 package 版本生成；其他字段修改须逐项报批。
_Avoid_: 脚本注释、头部注释
